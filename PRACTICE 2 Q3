#include <stdio.h>
#include <unistd.h>

int main()
{
    int pid = fork();

    if (pid == 0)
    {
        printf("Child Process\n");
        execlp("ls", "ls", "-l", NULL);
    }
    else
    {
        printf("Parent Process\n");
    }

    return 0;
}
